services:
  - type: web
    name: alberto-social-v2
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python -m backend.server"
    envVars:
      - key: PORT
        value: 8000
